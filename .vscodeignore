# ── Source files (compiled by bun build / webpack into dist/) ───────────────
**/*.ts
**/*.mts
**/*.map
**/tsconfig.json
**/tsconfig.base.json
**/tsconfig.tsbuildinfo
webpack.config.js

# ── Test artifacts (compiled test files that end up in dist/) ────────────────
**/*.test.js
tests/
scripts/
test-data/

# ── Build & developer tooling ────────────────────────────────────────────────
biome.json
.eslintrc.json
build-standalone.sh
bunfig.toml
bun.lock
**/bun.lock
**/*.bun-build
user-settings.json

# ── Agent / AI context files ─────────────────────────────────────────────────
.claude/
CLAUDE.md
DESIGN.md
BACKLOG.md

# ── Markdown lint config ─────────────────────────────────────────────────────
.markdownlint.yaml
.markdownlint-cli2.yaml

# ── NeoVim / standalone-only files ──────────────────────────────────────────
nvim-routeros-lsp-init.lua
lsp-routeros-server
lsp-routeros-server*

# ── CI / VCS config ──────────────────────────────────────────────────────────
.github/
.gitignore
.travis.yml

# ── Editor / IDE config ──────────────────────────────────────────────────────
.vscode/

# ── Docs: CORS guide (not referenced by walkthrough) ─────────────────────────
docs/cors.md

# ── node_modules ─────────────────────────────────────────────────────────────
**/node_modules/

# ── Scratch / temp ───────────────────────────────────────────────────────────
.scratch/

# ── Unreferenced source assets ────────────────────────────────────────────────
icon.svg
